{
  "name": "@tokenring-ai/skills",
  "version": "0.2.0",
  "description": "Capability management system for tracking and routing tasks based on agent proficiency.",
  "dependencies": {
    "@tokenring-ai/agent": "workspace:*",
    "@tokenring-ai/chat": "workspace:*",
    "@tokenring-ai/utility": "workspace:*",
    "zod": "^4.4.3"
  },
  "exports": {
    ".": "./index.ts",
    "./*": "./*.ts"
  },
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.1"
  },
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  }
}
